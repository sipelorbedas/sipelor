name: sipelor
description: "SIPELOR BEDAS — Sistem Pemesanan Lapangan Olahraga DISPORA Kabupaten Bandung."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+2

environment:
  sdk: ^3.10.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.0
  flutter_svg: ^2.2.3
  # ⬆️ UPDATED: Critical dependency updates
  supabase_flutter: ^2.6.0  # Updated from ^2.5.0 for bug fixes and performance
  sentry_flutter: ^9.10.0   # Error tracking and monitoring
  
  # 🆕 NEW: Architecture improvements
  flutter_riverpod: ^2.6.1       # State management
  get_it: ^8.0.2                 # Dependency injection
  freezed_annotation: ^2.4.4     # Immutable models (annotations)
  
  # Existing dependencies
  flutter_dotenv: ^6.0.0
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  qr_flutter: ^4.1.0
  file_picker: ^10.3.8
  screenshot: ^3.0.0
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  gal: ^2.3.0
  url_launcher: ^6.3.2
  shared_preferences: ^2.3.5
  flutter_ringtone_player: ^4.0.0+3
  vibration: ^3.1.5
  fl_chart: ^1.1.1
  csv: ^6.0.0
  intl: ^0.20.2
  local_auth: ^3.0.0
  flutter_secure_storage: ^10.0.0
  crypto: ^3.0.5
  encrypt: ^5.0.3
  dio: ^5.4.1
  http_certificate_pinning: ^3.0.1
  flutter_local_notifications: ^20.0.0
  app_links: ^6.3.4
  intro_slider: ^4.2.1
  tutorial_coach_mark: ^1.2.11
  pdf: ^3.11.1
  printing: ^5.13.4
  google_maps_flutter: ^2.10.0
  
  # 🆕 NEW: Offline mode
  connectivity_plus: ^6.1.1     # Network connectivity detection (Windows/Android/iOS/Web)

  # 🆕 NEW: Security and testing utilities
  device_info_plus: ^11.1.0      # For OWASP platform checks
  package_info_plus: ^8.1.0      # For security version checks
  http: ^1.2.2                   # For security header validation
  path: ^1.9.0                   # Path manipulation utilities
  speech_to_text: ^7.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.1
  
  # Testing dependencies
  mockito: ^5.4.4
  build_runner: ^2.4.8
  integration_test:
    sdk: flutter
  faker: ^2.1.0
  test: ^1.25.2
  
  # 🆕 NEW: Code generation for architecture
  freezed: ^2.5.7                # Immutable models (code generation)
  json_serializable: ^6.8.0      # JSON serialization
  riverpod_generator: ^2.4.3     # Provider code generation
  
  # 🆕 NEW: Advanced testing
  patrol: ^3.11.2                # E2E testing framework

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
    - assets/icons/admin/
    - assets/images/admin/
    - assets/sounds/
    - public/images/
    - public/icons/
    # ⚠️  SECURITY CRITICAL:
    # Baris ".env" di bawah HANYA untuk flutter run (development).
    # WAJIB dihapus/dikomentari sebelum production build agar tidak ter-bundle ke APK.
    #
    # Production build command (gunakan scripts/build_production.bat):
    #   flutter build apk --release \
    #     --dart-define=SUPABASE_URL=... \
    #     --dart-define=SUPABASE_ANON_KEY=... \
    #     --dart-define=SENTRY_DSN=... \
    #     --obfuscate --split-debug-info=build/debug-info/
    #
    # HAPUS KOMENTAR DARI BARIS INI UNTUK DEVELOPMENT:
    - .env  # ⚠️  DIKOMENTARI untuk production build

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# App Icon Configuration
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/sipelor.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/sipelor.png"
